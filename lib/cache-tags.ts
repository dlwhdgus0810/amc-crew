/**
 * 요청 사이에 남겨 두는 것들의 이름표.
 *
 * unstable_cache로 담아 둔 답을 언제 버릴지 정하는 데 쓴다. 담는 쪽(lib/db/*)과
 * 버리는 쪽(모임을 바꾸는 라우트들)이 같은 글자를 써야 해서 여기 모아 둔다 —
 * 한쪽에서 오타가 나면 화면이 옛날 값을 계속 들고 있게 되는데, 그건 눈에 잘 안 띈다.
 */

/**
 * 모임·참가자가 바뀌면 지운다.
 *
 * 카테고리별 다음 모임 요약과 주최·참가 순위가 여기 걸려 있다. 둘 다 여러 모임을
 * 가로질러 세는 것이라, 어느 모임 하나가 바뀌어도 답이 달라진다.
 */
export const POSTS_TAG = 'posts';
