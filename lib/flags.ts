/**
 * 켜고 끄는 스위치.
 *
 * 만들어 두긴 했는데 지금은 안 보여줄 기능을 여기서 끈다. 코드를 지우지 않는 이유는
 * 되살릴 때 다시 만들지 않기 위해서고, 화면 여기저기에 `false &&`를 흩어 놓지 않는 이유는
 * 나중에 어디를 켜야 하는지 찾을 수 없게 되기 때문이다.
 *
 * DB를 import하지 않는다 — 화면(클라이언트 컴포넌트)도 읽으므로, lib/db/*에 두면
 * drizzle과 PGlite가 브라우저 번들에 딸려 온다. lib/notif-kinds.ts와 같은 이유다.
 */

/**
 * 친구끼리 「지금 접속 중」을 보여줄지.
 *
 * 꺼 두면 친구 화면의 접속 중 목록과 초록 점, 친구별 「내 접속 상태」 스위치가 사라지고,
 * 서버도 그 값을 아예 안 내보낸다(lib/db/friends.ts). 화면에서만 가리면 개발자 도구로
 * 그대로 읽히니, 가릴 거면 보내지도 않는다.
 *
 * 신호는 그대로 쌓인다 — 관리자 화면의 접속 기록은 이 스위치와 무관하게 돌아간다.
 * 그래서 다시 켜면 그동안의 기록이 비어 있지 않다.
 */
export const FRIEND_PRESENCE = false;
