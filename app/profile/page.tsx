/*
 * 이 탭의 화면은 app/tab-deck.tsx가 그린다 (app/profile/profile-page.tsx).
 *
 * 다섯 탭을 레이아웃 쪽에서 한 번만 마운트해두고 안 내리기 때문이다 —
 * 탭을 옮길 때마다 다시 마운트하면 데이터를 다시 불러오느라 한 번 깜빡인다.
 * 이 파일은 주소를 여는 자리만 맡는다.
 */
export default function ProfileRoute() {
  return null;
}
